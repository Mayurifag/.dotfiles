Windows Registry Editor Version 5.00

; Disable saving attachment zone metadata so downloaded files don't get
; marked as untrusted, which helps prevent warning popups and reduces
; extra clicks when launching those files.

[HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Policies\Attachments]
"SaveZoneInformation"=dword:00000001
