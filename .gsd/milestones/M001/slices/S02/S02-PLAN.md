# S02: PowerShell Profile

**Goal:** A chezmoi-managed PowerShell 7 profile deploys correctly on Windows with mise, zoxide, and starship activation, shared env vars (including GEMINI_API_KEY via ejson), and all 8 shared alias groups rendered as PS functions.
**Demo:** Opening a new PowerShell session: `mise --version` works, zoxide and starship active, `$env:EDITOR` set, `ll`/`gs` alias groups execute correct underlying commands.

## Must-Haves


## Tasks


## Files Likely Touched

