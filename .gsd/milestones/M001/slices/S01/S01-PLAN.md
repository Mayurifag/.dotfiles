# S01: Template Infrastructure

**Goal:** The chezmoi template pipeline scaffold exists with correct conventions — `.chezmoidata.toml`, `.chezmoiignore` Documents/ gate, and all `.chezmoitemplates/` fragments in place so downstream shell files can be authored without structural mistakes.
**Demo:** `chezmoi data` resolves `sharedEnv` key; `chezmoi apply --dry-run` on Linux produces no `Documents/` entries; `chezmoi execute-template` renders alias and env-var fragments without error.

## Must-Haves


## Tasks


## Files Likely Touched

