# S03: Git Bash Configuration

**Goal:** A chezmoi-managed `dot_bashrc.tmpl` gives Git Bash on Windows mise activation, correct SSH agent routing to the Windows OpenSSH service (no second agent spawned), shared env vars, and all shared alias groups.
**Demo:** `mise --version` works in Git Bash; `ssh -T git@github.com` succeeds via Windows OpenSSH pipe; `echo $GEMINI_API_KEY` and `ll`/`gs` resolve correctly.

## Must-Haves


## Tasks


## Files Likely Touched

