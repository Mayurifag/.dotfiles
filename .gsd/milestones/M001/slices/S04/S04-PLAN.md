# S04: Zsh Alignment

**Goal:** `exact_zsh/40-aliases.zsh` converted to `40-aliases.zsh.tmpl` consuming the shared `aliases_posix` fragment — single-source-of-truth complete; adding an alias in the fragment now renders it in all three shells simultaneously.
**Demo:** All existing zsh aliases work after conversion; Linux-only aliases (yay, brewfile, bundleantidote, updatedesktopdb) remain in the zsh template only and are absent from PS/Git Bash output.

## Must-Haves


## Tasks


## Files Likely Touched

